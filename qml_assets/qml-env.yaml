version: 0.0.4
setup:
  structure:
    - data:
      - data_conf:
      - winequality-red.csv
    - src:
      - ml_pipelines:
      - ml_primitives:
    - Pipfile
    - Pipfile.lock
  run:
    - project_initializer
watchdog:
  - data:
      on_create:
        - add_to_DVC
        - generate_profile
commands:
  - inspect-data
  - stuff